# 클라이언트 연결 관리 분석

## 현재 클라이언트 측 동작

### 1. 연결 종료 시점
- 화면이 dispose될 때 `_databaseService.dispose()` 호출
- `DatabaseService.dispose()` → `HttpRequestHandler.dispose()` → `_httpClient.close()`
- HTTP 클라이언트가 닫히면 **모든 연결이 즉시 닫힘**

### 2. Keep-Alive 헤더 사용
```dart
'Connection': 'keep-alive'  // 헤더에 포함
```

### 3. 연결 풀 설정
```dart
connectionTimeout: 30초
idleTimeout: 30초  // 30초 동안 사용되지 않으면 연결 닫힘
```

## 문제점 분석

### 문제 1: 클라이언트가 연결을 닫을 때 서버에 알림 없음
- 클라이언트가 `dispose()`를 호출하면 HTTP 클라이언트가 닫힘
- 서버 측에서는 클라이언트가 연결을 끊었다는 것을 **즉시 알 수 없음**
- 서버 측 연결 풀에 연결이 남아있을 수 있음

### 문제 2: idleTimeout이 짧음
- 현재 30초로 설정되어 있음
- 요청 간격이 30초 이상이면 연결이 닫히고 새로 생성됨
- 서버 측 연결 풀과 동기화되지 않을 수 있음

### 문제 3: 서버 측 타임아웃과 불일치
- 서버가 클라이언트 연결을 타임아웃으로 정리하는 시간과 클라이언트의 idleTimeout이 다를 수 있음

## 서버 측에서 확인해야 할 사항

### 1. 연결 풀 타임아웃 설정
서버 측 코드에서 다음을 확인:
```javascript
// 예시: PostgreSQL 연결 풀 설정
const pool = new Pool({
  max: 20,                    // 최대 연결 수
  idleTimeoutMillis: 30000,  // 30초 동안 사용되지 않으면 연결 닫기
  connectionTimeoutMillis: 2000,  // 연결 타임아웃
});
```

### 2. 클라이언트 연결 감지
서버가 클라이언트 연결이 끊어졌는지 감지하는 방법:
- **HTTP Keep-Alive**: 클라이언트가 연결을 유지하면 서버도 유지
- **타임아웃 기반 정리**: 일정 시간 동안 요청이 없으면 연결 정리
- **소켓 에러 감지**: 클라이언트가 연결을 끊으면 소켓 에러 발생

### 3. 연결 풀 정리 로직
서버 측에서 다음을 확인:
```javascript
// 클라이언트 연결이 끊어졌을 때 연결 풀 정리
pool.on('remove', (client) => {
  console.log('클라이언트 연결 제거됨');
});

// 타임아웃 기반 정리
setInterval(() => {
  pool.query('SELECT NOW()', (err, res) => {
    if (err) {
      // 연결이 끊어졌으면 정리
      pool.end();
    }
  });
}, 30000);
```

## 가능한 시나리오

### 시나리오 1: 서버가 클라이언트 연결을 타임아웃으로 정리
- 클라이언트가 30초 이상 요청을 보내지 않음
- 서버가 연결을 타임아웃으로 정리
- **문제**: 클라이언트가 여전히 연결을 유지하고 있다고 생각할 수 있음

### 시나리오 2: 클라이언트가 dispose() 호출
- 화면이 닫히면 `dispose()` 호출
- HTTP 클라이언트가 닫힘
- **문제**: 서버가 이를 즉시 감지하지 못할 수 있음

### 시나리오 3: 네트워크 연결 끊김
- 네트워크 문제로 연결이 끊김
- 서버가 소켓 에러를 감지하여 연결 정리
- **정상 동작**

## 해결 방법

### 1. 클라이언트 측 개선
```dart
// dispose() 호출 전에 서버에 연결 종료 알림
Future<void> dispose() async {
  try {
    // 서버에 연결 종료 알림 (선택사항)
    await _databaseService.disconnectDatabase();
  } catch (e) {
    // 에러 무시 (이미 연결이 끊어졌을 수 있음)
  }
  _httpClient.close();
}
```

### 2. 서버 측 개선
- **타임아웃 기반 정리**: 일정 시간 동안 요청이 없으면 연결 정리
- **소켓 에러 감지**: 클라이언트 연결이 끊어졌을 때 즉시 감지
- **연결 풀 모니터링**: 연결 풀 상태를 모니터링하고 정리

### 3. Keep-Alive 최적화
- 클라이언트와 서버의 타임아웃 설정을 동기화
- 적절한 idleTimeout 설정 (너무 짧지도 길지도 않게)

## 권장 사항

1. **서버 측 로그 확인**: 연결 풀 상태와 타임아웃 설정 확인
2. **타임아웃 동기화**: 클라이언트와 서버의 타임아웃 설정을 일치시키기
3. **연결 풀 모니터링**: 서버 측에서 연결 풀 상태를 모니터링하고 정리

## 확인할 서버 측 코드

서버 측 코드에서 다음을 확인하세요:

1. **PostgreSQL 연결 풀 설정**
   - `idleTimeoutMillis` 값
   - `max` 연결 수
   - 타임아웃 기반 정리 로직

2. **HTTP 서버 설정**
   - Keep-Alive 타임아웃
   - 연결 종료 감지 로직

3. **연결 풀 정리 로직**
   - 클라이언트 연결이 끊어졌을 때 정리하는 로직
   - 타임아웃 기반 정리 로직

