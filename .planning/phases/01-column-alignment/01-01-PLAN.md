---
phase: 01-column-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/reports/stocks_report_view.dart
  - lib/widgets/resizable_data_table.dart
autonomous: true
requirements: [ALIGN-01, ALIGN-02, ALIGN-03]

must_haves:
  truths:
    - "Stocks 리포트 헤더와 데이터 행이 동일한 칼럼 폭을 사용하여 픽셀 단위로 정렬됨"
    - "칼럼 리사이즈 핸들 드래그 시 헤더와 데이터 행 폭이 동시에 업데이트됨"
    - "가로 스크롤 시 헤더와 데이터 행이 동일한 x 오프셋으로 이동함"
    - "세로 스크롤 시 고정 헤더가 데이터 행 칼럼과 정렬 유지함"
    - "무한스크롤(_onScroll)이 정상 작동함"
  artifacts:
    - path: "lib/screens/reports/stocks_report_view.dart"
      provides: "scrollController 전달 및 칼럼 폭 로딩 로직 수정"
      contains: "scrollController: _scrollController"
    - path: "lib/widgets/resizable_data_table.dart"
      provides: "가로/세로 스크롤 분리 — 외부 scrollController는 세로 스크롤 전용"
      contains: "_verticalScrollController"
  key_links:
    - from: "lib/screens/reports/stocks_report_view.dart"
      to: "lib/widgets/resizable_data_table.dart"
      via: "scrollController parameter"
      pattern: "scrollController:\\s*_scrollController"
    - from: "lib/widgets/resizable_data_table.dart"
      to: "lib/widgets/resizable_data_table.dart"
      via: "_headerScrollController <-> _dataScrollController listener sync"
      pattern: "_syncHeaderToData|_syncDataToHeader"
    - from: "lib/screens/reports/stocks_report_view.dart"
      to: "lib/services/stocks_column_width_storage.dart"
      via: "initState column width loading"
      pattern: "StocksColumnWidthStorage\\.load"
---

<objective>
Stocks 리포트의 헤더/행 칼럼 정렬 불일치를 근본적으로 수정한다.

Purpose: 현재 ResizableDataTable은 구조적으로 올바르지만 (1) 외부 scrollController 전달이 누락되어 무한스크롤과 세로 스크롤 제어가 분리됨, (2) 칼럼 폭 로딩이 build() 내 addPostFrameCallback에서 실행되어 매 rebuild마다 불필요한 비동기 로딩이 트리거됨. 이 두 가지를 수정하고 헤더/데이터 행의 폭 동기화를 검증한다.

Output: 수정된 stocks_report_view.dart와 resizable_data_table.dart
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-column-alignment/01-CONTEXT.md
@.planning/phases/01-column-alignment/01-RESEARCH.md

<interfaces>
<!-- ResizableDataTable 핵심 인터페이스 — executor가 수정할 대상 -->

From lib/widgets/resizable_data_table.dart:
```dart
class ResizableDataTable extends StatefulWidget {
  final List<TableColumnDef> columns;
  final List<List<Widget>> rows;
  final Map<String, double> columnWidths;
  final void Function(String key, double newWidth) onColumnResize;
  final String? sortColumn;
  final bool sortAscending;
  final void Function(String column, bool ascending)? onSort;
  final Color headerColor;
  final Widget? footerWidget;
  final bool isLoadingMore;
  final void Function(int index)? onRowTap;
  final void Function(int index)? onRowDoubleTap;
  final int? selectedRowIndex;
  final int? editedRowIndex;
  /// 현재: 가로+세로 구분 없이 단일 scrollController
  /// 수정 후: 세로 스크롤 전용 verticalScrollController로 변경
  final ScrollController? scrollController;
}
```

From lib/screens/reports/stocks_report_view.dart (수정 대상 부분):
```dart
// 현재 _buildStocksContent() — scrollController 전달 안 됨 (line 431-454)
return ResizableDataTable(
  columns: StocksBuilder.buildColumnDefs(),
  rows: StocksBuilder.buildRows(dataList),
  columnWidths: mergedColumnWidths,
  onColumnResize: (key, newWidth) { ... },
  // scrollController: 누락!
  headerColor: stocksColor,
  isLoadingMore: _isLoadingMoreStocks,
);

// 현재 addPostFrameCallback 문제 (line 412-421)
if (_stocksColumnWidthsDbKey != dbKey) {
  WidgetsBinding.instance.addPostFrameCallback((_) async {
    final loaded = await StocksColumnWidthStorage.load(dbKey);
    ...
  });
}
```

From lib/widgets/stocks_builder.dart:
```dart
static List<TableColumnDef> buildColumnDefs() => [/* 19 columns */];
static List<List<Widget>> buildRows(List<dynamic> data) => ...;
```

From lib/services/stocks_column_width_storage.dart:
```dart
static Future<Map<String, double>?> load(String databaseName) async { ... }
static Future<void> save(String databaseName, Map<String, double> widths) async { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResizableDataTable 세로/가로 스크롤 분리 및 StocksReportView scrollController 연결</name>
  <files>lib/widgets/resizable_data_table.dart, lib/screens/reports/stocks_report_view.dart</files>
  <read_first>
    - lib/widgets/resizable_data_table.dart (전체 — 수정 대상, 현재 구조 확인)
    - lib/screens/reports/stocks_report_view.dart (전체 — scrollController 전달 및 _onScroll 연동 확인)
    - lib/widgets/stocks_builder.dart (buildColumnDefs, buildRows 시그니처 확인)
  </read_first>
  <action>
**resizable_data_table.dart 수정 (per D-05, D-06):**

현재 `scrollController` 파라미터는 가로 스크롤(`_dataScrollController`)에 연결되어 있다. 그러나 StocksReportView의 `_scrollController`는 `_onScroll()`에서 세로 스크롤 위치를 감지하여 무한스크롤을 트리거한다. 따라서 외부 scrollController는 세로 스크롤에 연결되어야 한다.

1. `ResizableDataTable`의 `scrollController` 파라미터 이름을 변경하지 않는다 (기존 API 유지). 대신 내부에서 이 controller를 세로 스크롤(ListView)에 연결한다.

2. `_ResizableDataTableState` 수정:
   - `_headerScrollController`: 가로 스크롤용 (변경 없음, 내부 생성)
   - `_dataHorizontalScrollController`: 가로 스크롤용 (변경: 이름 변경, 항상 내부 생성)
   - 세로 스크롤: `widget.scrollController`가 있으면 `ListView.builder`의 `controller`로 직접 전달, 없으면 controller 미지정 (ListView 기본 동작)

3. `initState()` 수정:
   ```dart
   @override
   void initState() {
     super.initState();
     _headerScrollController = ScrollController();
     _dataHorizontalScrollController = ScrollController();
     _headerScrollController.addListener(_syncHeaderToData);
     _dataHorizontalScrollController.addListener(_syncDataToHeader);
   }
   ```

4. `dispose()` 수정:
   ```dart
   @override
   void dispose() {
     _headerScrollController.removeListener(_syncHeaderToData);
     _dataHorizontalScrollController.removeListener(_syncDataToHeader);
     _headerScrollController.dispose();
     _dataHorizontalScrollController.dispose();
     super.dispose();
   }
   ```
   - `_ownsDataScrollController` 플래그 제거 (가로 스크롤은 항상 내부 소유)

5. `_syncHeaderToData()` / `_syncDataToHeader()` 수정:
   - `_dataScrollController` 참조를 `_dataHorizontalScrollController`로 변경

6. `build()` 내 데이터 영역 수정:
   ```dart
   Expanded(
     child: SingleChildScrollView(
       controller: _dataHorizontalScrollController,  // 가로 스크롤
       scrollDirection: Axis.horizontal,
       physics: const ClampingScrollPhysics(),
       child: SizedBox(
         width: _totalContentWidth(),
         child: ListView.builder(
           controller: widget.scrollController,  // 세로 스크롤 (외부 또는 null)
           itemCount: widget.rows.length,
           itemBuilder: (context, index) { /* 기존 코드 그대로 */ },
         ),
       ),
     ),
   ),
   ```

**stocks_report_view.dart 수정 (per D-05, D-06, D-07):**

7. `_buildStocksContent()` 메서드 내 `ResizableDataTable` 생성 부분에 `scrollController: _scrollController` 추가:
   ```dart
   return ResizableDataTable(
     columns: StocksBuilder.buildColumnDefs(),
     rows: StocksBuilder.buildRows(dataList),
     columnWidths: mergedColumnWidths,
     onColumnResize: (key, newWidth) {
       setState(() {
         _stocksColumnWidths ??= {};
         _stocksColumnWidths![key] = newWidth;
       });
       StocksColumnWidthStorage.save(dbKey, _stocksColumnWidths!);
     },
     scrollController: _scrollController,  // 추가: 세로 스크롤 + 무한스크롤 연동
     sortColumn: _stocksSortColumn,
     sortAscending: _stocksSortAscending,
     onSort: (column, ascending) { /* 기존 코드 */ },
     headerColor: stocksColor,
     isLoadingMore: _isLoadingMoreStocks,
   );
   ```

주석은 한국어로 작성. 변수/함수명은 영어.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && dart analyze lib/widgets/resizable_data_table.dart lib/screens/reports/stocks_report_view.dart 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - resizable_data_table.dart contains `_dataHorizontalScrollController` (renamed from `_dataScrollController`)
    - resizable_data_table.dart contains `controller: widget.scrollController` inside ListView.builder
    - resizable_data_table.dart does NOT contain `_ownsDataScrollController`
    - resizable_data_table.dart `_syncHeaderToData` references `_dataHorizontalScrollController`
    - resizable_data_table.dart `_syncDataToHeader` references `_dataHorizontalScrollController`
    - stocks_report_view.dart contains `scrollController: _scrollController` in ResizableDataTable constructor call
    - `dart analyze` reports no errors for both files
  </acceptance_criteria>
  <done>ResizableDataTable이 외부 scrollController를 세로 스크롤(ListView)에 연결하고, 가로 스크롤은 내부 컨트롤러 쌍(header + data horizontal)으로 동기화. StocksReportView가 _scrollController를 전달하여 무한스크롤 _onScroll이 정상 작동.</done>
</task>

<task type="auto">
  <name>Task 2: 칼럼 폭 로딩을 initState로 이동 및 addPostFrameCallback 루프 제거</name>
  <files>lib/screens/reports/stocks_report_view.dart</files>
  <read_first>
    - lib/screens/reports/stocks_report_view.dart (Task 1 수정 후 상태 확인)
    - lib/services/stocks_column_width_storage.dart (load/save API 확인)
  </read_first>
  <action>
**stocks_report_view.dart 수정 (per D-04, Pitfall 4 수정):**

현재 `_buildStocksContent()` (build 내 호출) 에서 `addPostFrameCallback` + `setState`로 칼럼 폭을 로딩한다 (line 412-421). 이는 매 rebuild마다 새 callback을 등록하는 anti-pattern이다. `_stocksColumnWidthsDbKey` 가드가 있지만 첫 로딩 전까지는 매번 실행된다.

1. `initState()`에 칼럼 폭 로딩 추가:
   ```dart
   @override
   void initState() {
     super.initState();
     _databaseService = DatabaseService(serverUrl: widget.serverUrl);
     _scrollController.addListener(_onScroll);
     _filteringWordController.addListener(_onFilteringWordChangedDebounced);
     // ... 기존 코드 ...
     _loadTiposAndTemporadas();
     _loadData();
     _loadColumnWidths();  // 추가
   }
   ```

2. 새 메서드 `_loadColumnWidths()` 추가:
   ```dart
   /// 칼럼 폭 로딩 (initState에서 1회 호출)
   Future<void> _loadColumnWidths() async {
     const dbKey = '';  // Stocks 전용 뷰는 DB 키 없이 하나의 저장소 사용
     final loaded = await StocksColumnWidthStorage.load(dbKey);
     if (mounted) {
       setState(() {
         _stocksColumnWidthsDbKey = dbKey;
         _stocksColumnWidths = loaded;
       });
     }
   }
   ```

3. `_buildStocksContent()` 에서 `addPostFrameCallback` 블록 전체 제거 (line 412-421의 `if (_stocksColumnWidthsDbKey != dbKey) { ... }` 블록 삭제).

4. `_buildStocksContent()`의 나머지 로직 유지:
   ```dart
   Widget _buildStocksContent(Map<String, dynamic> data) {
     // stocksColor 결정 로직 (기존 유지)
     Color stocksColor = Colors.orange;
     if (data.containsKey('filters') && data['filters'] is Map) {
       final filters = data['filters'] as Map<String, dynamic>;
       stocksColor = ReportUtils.isBcolorviewEnabled(filters['bcolorview']) ? Colors.orange : Colors.lightBlue;
     } else if (data.containsKey('bcolorview')) {
       stocksColor = ReportUtils.isBcolorviewEnabled(data['bcolorview']) ? Colors.orange : Colors.lightBlue;
     }
     // mergedColumnWidths 계산 (기존 유지)
     final defaults = {
       for (final col in StocksBuilder.buildColumnDefs()) col.key: col.defaultWidth
     };
     final mergedColumnWidths = Map<String, double>.from(defaults)
       ..addAll(_stocksColumnWidths ?? {});
     final dataList = data['data'] as List? ?? [];
     return ResizableDataTable(
       // ... (Task 1에서 수정된 코드 포함)
     );
   }
   ```

5. `_stocksColumnWidthsDbKey` 필드는 유지 (save 시 여전히 사용됨). 단, `_buildStocksContent`에서의 가드 체크는 제거.

주석은 한국어로 작성.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && dart analyze lib/screens/reports/stocks_report_view.dart 2>&1 | head -20 && echo "---CHECK_NO_POSTFRAME---" && grep -n "addPostFrameCallback.*StocksColumnWidthStorage" lib/screens/reports/stocks_report_view.dart || echo "OK: no addPostFrameCallback for column widths"</automated>
  </verify>
  <acceptance_criteria>
    - stocks_report_view.dart contains `_loadColumnWidths()` method definition
    - stocks_report_view.dart `initState()` contains `_loadColumnWidths()`
    - stocks_report_view.dart does NOT contain `addPostFrameCallback` + `StocksColumnWidthStorage.load` in `_buildStocksContent`
    - stocks_report_view.dart `_buildStocksContent` does NOT contain `WidgetsBinding.instance.addPostFrameCallback`  (the only remaining addPostFrameCallback should be in `build()` for `onFilterBarReady`)
    - `dart analyze` reports no errors
  </acceptance_criteria>
  <done>칼럼 폭 로딩이 initState에서 1회만 실행되고, build 내 addPostFrameCallback 루프가 제거됨. 매 rebuild마다 불필요한 비동기 로딩이 트리거되지 않음.</done>
</task>

<task type="auto">
  <name>Task 3: 전체 빌드 검증 및 칼럼 정렬 디버그 확인</name>
  <files>lib/widgets/resizable_data_table.dart</files>
  <read_first>
    - lib/widgets/resizable_data_table.dart (Task 1 수정 후 최종 상태)
    - lib/screens/reports/stocks_report_view.dart (Task 2 수정 후 최종 상태)
  </read_first>
  <action>
**전체 프로젝트 빌드 검증:**

1. `flutter analyze` 실행하여 전체 프로젝트에 분석 오류 없음을 확인.

2. `resizable_data_table.dart`의 `_totalContentWidth()` 수식이 헤더 Row와 데이터 Row에 동일하게 적용되는지 코드 레벨 확인:
   - 헤더: `Padding(horizontal: 16)` + Row children = `sum(colWidths) + (n-1)*8 + 32`
   - 데이터: `_totalContentWidth()` = `sum(colWidths) + (n-1)*8 + 32`
   - 데이터 Row: `Padding(horizontal: 16)` + Row children 내 SizedBox(width: colWidth) + SizedBox(width: 8) gap
   이 세 곳이 동일 수식이면 정렬이 보장된다.

3. 헤더 영역의 패딩이 `horizontal: 16, vertical: 12`이고 데이터 영역은 `horizontal: 16, vertical: 5`인데, horizontal이 동일(16)하므로 가로 정렬에 영향 없음을 확인.

4. `ResizableDataTable`에 임시 디버그 assert를 `build()` 첫 줄에 추가하여 헤더와 데이터의 칼럼 수가 일치하는지 확인:
   ```dart
   @override
   Widget build(BuildContext context) {
     // 디버그: 칼럼 수와 행 셀 수 일치 확인
     assert(
       widget.rows.isEmpty || widget.rows.every((row) => row.length == widget.columns.length),
       'ResizableDataTable: 행의 셀 수(${widget.rows.isNotEmpty ? widget.rows.first.length : 0})가 '
       '칼럼 수(${widget.columns.length})와 불일치',
     );
     return Column(
       // ... 기존 코드
     );
   }
   ```

5. 전체 빌드 테스트:
   ```bash
   flutter build macos --debug 2>&1 | tail -5
   ```
   또는 분석만:
   ```bash
   flutter analyze
   ```

이 Task는 수정 사항의 정합성을 검증하는 최종 확인 단계.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && flutter analyze 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` exits with "No issues found!" or only info-level warnings (no errors)
    - resizable_data_table.dart `build()` contains `assert(widget.rows.isEmpty || widget.rows.every`
    - resizable_data_table.dart header Padding horizontal is 16 AND data row Padding horizontal is 16 (both identical)
  </acceptance_criteria>
  <done>전체 프로젝트가 분석 오류 없이 빌드 가능하고, 헤더/데이터 칼럼 수 일치 assert가 추가되어 런타임 불일치를 즉시 감지. 가로 패딩 동일성 확인 완료.</done>
</task>

</tasks>

<verification>
1. `dart analyze lib/widgets/resizable_data_table.dart lib/screens/reports/stocks_report_view.dart` — 오류 없음
2. `flutter analyze` — 전체 프로젝트 분석 오류 없음
3. `grep -n "scrollController: _scrollController" lib/screens/reports/stocks_report_view.dart` — 매치 있음
4. `grep -n "_dataHorizontalScrollController" lib/widgets/resizable_data_table.dart` — 매치 있음 (이름 변경 확인)
5. `grep -n "addPostFrameCallback" lib/screens/reports/stocks_report_view.dart` — StocksColumnWidthStorage 관련 매치 없음 (onFilterBarReady 관련만 남음)
</verification>

<success_criteria>
- ResizableDataTable이 외부 scrollController를 세로 스크롤(ListView)에 연결
- 가로 스크롤 동기화가 내부 _headerScrollController / _dataHorizontalScrollController 쌍으로 작동
- StocksReportView가 _scrollController를 ResizableDataTable에 전달하여 무한스크롤 연동
- 칼럼 폭 로딩이 initState에서 1회만 실행
- 전체 프로젝트 flutter analyze 통과
</success_criteria>

<output>
After completion, create `.planning/phases/01-column-alignment/01-01-SUMMARY.md`
</output>
