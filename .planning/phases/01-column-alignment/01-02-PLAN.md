---
phase: 01-column-alignment
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [ALIGN-01, ALIGN-02, ALIGN-03]

must_haves:
  truths:
    - "Stocks 리포트 헤더 칼럼과 데이터 행 칼럼이 시각적으로 완벽히 정렬됨"
    - "칼럼 리사이즈 드래그 후 헤더/행 폭이 어긋남 없이 동시 업데이트됨"
    - "가로 스크롤 끝까지 이동해도 헤더와 데이터 행이 정렬 유지됨"
    - "세로 스크롤 시 고정 헤더가 데이터 칼럼과 정렬 유지됨"
    - "무한스크롤이 정상 작동함 (하단 도달 시 추가 데이터 로딩)"
  artifacts: []
  key_links: []
---

<objective>
Plan 01에서 수정한 칼럼 정렬 코드가 실제 디바이스/시뮬레이터에서 올바르게 작동하는지 시각적으로 검증한다.

Purpose: 코드 분석만으로는 확인할 수 없는 시각적 정렬과 인터랙션 동작을 사용자가 직접 확인
Output: 사용자 승인 또는 수정 필요 사항 피드백
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-column-alignment/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stocks 리포트 칼럼 정렬 시각적 검증</name>
  <files>lib/widgets/resizable_data_table.dart, lib/screens/reports/stocks_report_view.dart</files>
  <action>
    사용자가 앱을 실행하여 Plan 01의 수정 사항을 시각적으로 검증한다. Claude가 추가로 수행할 작업 없음 — 순수 사용자 검증 checkpoint.

    Plan 01에서 수정한 내용:
    1. ResizableDataTable — 외부 scrollController가 세로 스크롤(ListView)에 연결, 가로 스크롤은 내부 컨트롤러 쌍으로 동기화
    2. StocksReportView — _scrollController를 ResizableDataTable에 전달하여 무한스크롤 연동
    3. 칼럼 폭 로딩이 initState에서 1회만 실행 (build 내 addPostFrameCallback 루프 제거)
    4. 헤더/데이터 칼럼 수 일치 assert 추가
  </action>
  <read_first>
    - lib/widgets/resizable_data_table.dart (수정 후 최종 상태 확인)
    - lib/screens/reports/stocks_report_view.dart (수정 후 최종 상태 확인)
  </read_first>
  <verify>
    사용자 시각적 확인:

    **ALIGN-01 검증 (칼럼 폭 일치):**
    1. Stocks 리포트를 연다
    2. 헤더 텍스트(Codigo, Descripcion, Totaling...)와 데이터 행의 해당 칼럼이 수직으로 정렬되는지 확인
    3. 특히 오른쪽 끝 칼럼(Sucursal, ID Codigo1)에서 어긋남이 없는지 확인

    **ALIGN-02 검증 (리사이즈 동기화):**
    4. 칼럼 헤더의 리사이즈 핸들(||)을 드래그하여 폭을 변경
    5. 드래그 중과 드래그 후 모두 헤더와 데이터 행 폭이 동시에 업데이트되는지 확인
    6. 리사이즈 후 좌우 인접 칼럼과의 정렬이 유지되는지 확인

    **ALIGN-03 검증 (스크롤 동기화):**
    7. 가로 스크롤: 오른쪽으로 끝까지 스크롤 — 헤더와 데이터가 동일 속도로 이동하는지 확인
    8. 가로 스크롤: 왼쪽으로 되돌아가며 중간 위치에서 정렬 확인
    9. 세로 스크롤: 아래로 스크롤하면서 고정 헤더가 데이터 칼럼과 정렬 유지되는지 확인
    10. 세로 스크롤 하단 도달 시 추가 데이터가 로딩되는지 확인 (무한스크롤)

    **회귀 검증:**
    11. 필터(tipo, temporada, filtering word)를 변경한 후 데이터 리로딩이 정상 작동하는지 확인
    12. 앱을 다시 시작한 후 리사이즈한 칼럼 폭이 복원되는지 확인 (persist)
  </verify>
  <acceptance_criteria>
    - 사용자가 ALIGN-01 항목 (1-3) 통과 확인
    - 사용자가 ALIGN-02 항목 (4-6) 통과 확인
    - 사용자가 ALIGN-03 항목 (7-10) 통과 확인
    - 사용자가 회귀 검증 항목 (11-12) 통과 확인
  </acceptance_criteria>
  <done>사용자가 "approved" 응답 또는 구체적 이슈를 피드백</done>
</task>

</tasks>

<verification>
사용자의 시각적 확인으로 ALIGN-01, ALIGN-02, ALIGN-03 요구사항 달성 여부를 최종 판단.
</verification>

<success_criteria>
- 사용자가 12개 검증 항목 모두 통과를 확인하고 "approved" 응답
- 또는 사용자가 발견한 문제를 피드백하여 추가 수정 Plan 생성
</success_criteria>

<output>
After completion, create `.planning/phases/01-column-alignment/01-02-SUMMARY.md`
</output>
