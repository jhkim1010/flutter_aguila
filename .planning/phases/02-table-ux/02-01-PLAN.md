---
phase: 02-table-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/api/stocks_api.dart
  - lib/services/database_service.dart
  - lib/screens/reports/stocks_report_view.dart
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03]

must_haves:
  truths:
    - "칼럼 헤더 클릭 시 오름차순/내림차순 정렬이 동작한다 (TBL-01, 이미 구현됨)"
    - "필터바(tipo/temporada/텍스트검색/color-talle)가 직관적으로 동작한다 (TBL-02, 이미 구현됨)"
    - "테이블 하단에 이전/다음 버튼, 페이지 번호, 페이지 크기 선택기가 표시된다"
    - "이전/다음 버튼으로 페이지 이동이 동작하고, 첫/마지막 페이지에서 비활성화된다"
    - "페이지 크기를 50/100/200으로 변경할 수 있고, 변경 시 첫 페이지로 돌아간다"
    - "필터/정렬 변경 시 자동으로 첫 페이지로 초기화된다"
    - "무한스크롤 관련 코드(_onScroll, _loadNextStocksPage, _stocksHasMore, _stocksNextMaxUtime, _isLoadingMoreStocks)가 제거되었다"
  artifacts:
    - path: "lib/services/api/stocks_api.dart"
      provides: "offset/limit 파라미터 기반 Stocks API"
      contains: "int? offset"
    - path: "lib/services/database_service.dart"
      provides: "offset/limit 파라미터 전달 facade"
      contains: "int? offset"
    - path: "lib/screens/reports/stocks_report_view.dart"
      provides: "페이지네이션 상태 관리 + 컨트롤 위젯"
      contains: "_currentPage"
  key_links:
    - from: "lib/screens/reports/stocks_report_view.dart"
      to: "lib/services/database_service.dart"
      via: "getStocksReport(offset: (_currentPage - 1) * _pageSize, limit: _pageSize)"
      pattern: "offset.*_currentPage.*_pageSize"
    - from: "lib/services/database_service.dart"
      to: "lib/services/api/stocks_api.dart"
      via: "getStocksReport(offset: offset, limit: limit)"
      pattern: "offset: offset.*limit: limit"
    - from: "lib/screens/reports/stocks_report_view.dart"
      to: "lib/widgets/resizable_data_table.dart"
      via: "footerWidget: _buildPaginationControls()"
      pattern: "footerWidget.*_buildPaginationControls"
---

<objective>
무한스크롤을 제거하고 페이지 기반 네비게이션으로 전환한다. API 레이어에 offset/limit 파라미터를 추가하고, StocksReportView에 페이지네이션 상태와 컨트롤 위젯을 구현한다.

Purpose: 사용자가 Stocks 리포트 데이터를 페이지 단위로 탐색할 수 있도록 한다 (D-03~D-06).
Output: offset/limit 기반 API 호출, 테이블 하단 페이지네이션 컨트롤 (이전/다음 + 페이지 번호 + 크기 선택기)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-table-ux/02-CONTEXT.md
@.planning/phases/02-table-ux/02-RESEARCH.md

<interfaces>
<!-- StocksApi 현재 시그니처 (변경 대상) -->
From lib/services/api/stocks_api.dart:
```dart
Future<Map<String, dynamic>> getStocksReport({
  String? filteringWord,
  Map<String, dynamic>? filters,
  String? maxUtime,       // 제거 대상
  String? sortColumn,
  bool? sortAscending,
}) async { ... }

Future<StocksResponse> getStocksReportTyped({
  String? filteringWord,
  Map<String, dynamic>? filters,
  String? maxUtime,       // 제거 대상
  String? sortColumn,
  bool? sortAscending,
}) async { ... }
```

From lib/services/database_service.dart:
```dart
Future<Map<String, dynamic>> getStocksReport({
  String? filteringWord,
  Map<String, dynamic>? filters,
  String? maxUtime,       // 제거 대상
  String? sortColumn,
  bool? sortAscending,
}) => _stocksApi.getStocksReport(...);

Future<StocksResponse> getStocksReportTyped({
  String? filteringWord,
  Map<String, dynamic>? filters,
  String? maxUtime,       // 제거 대상
  String? sortColumn,
  bool? sortAscending,
}) => _stocksApi.getStocksReportTyped(...);
```

From lib/widgets/resizable_data_table.dart:
```dart
class ResizableDataTable extends StatefulWidget {
  final Widget? footerWidget;      // 테이블 하단 슬롯 (페이지네이션 컨트롤 삽입 위치)
  final bool isLoadingMore;        // 무한스크롤용 (false 고정 또는 제거)
  final ScrollController? scrollController;  // 세로 스크롤용 (유지)
}
```

From lib/models/stocks_response.dart:
```dart
class StocksPagination {
  final int count;
  final int total;            // 전체 아이템 수 (totalPages 계산용)
  final bool hasMore;
  final String? nextMaxUtime; // 무한스크롤용 (무시 가능)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 레이어 offset/limit 파라미터 전환</name>
  <files>lib/services/api/stocks_api.dart, lib/services/database_service.dart</files>
  <read_first>
    - lib/services/api/stocks_api.dart (현재 maxUtime 파라미터 구조 확인)
    - lib/services/database_service.dart (facade 시그니처 확인)
    - lib/screens/helpers/report_data_loader.dart (레거시 파일 — maxUtime 참조 확인, 이 파일은 수정하지 않음)
  </read_first>
  <action>
**stocks_api.dart 변경 (per D-03):**

1. `getStocksReport()` 시그니처에서 `String? maxUtime` 파라미터를 `int? offset, int? limit`로 교체:
```dart
Future<Map<String, dynamic>> getStocksReport({
  String? filteringWord,
  Map<String, dynamic>? filters,
  int? offset,       // 신규: (page - 1) * pageSize
  int? limit,        // 신규: pageSize (50, 100, 200)
  String? sortColumn,
  bool? sortAscending,
}) async {
```

2. 메서드 본문에서 `maxUtime` 관련 코드를 `offset`/`limit`로 교체:
```dart
// 제거:
// if (maxUtime != null && maxUtime.isNotEmpty) {
//   queryParams['max_utime'] = maxUtime;
// }

// 추가:
if (offset != null) queryParams['offset'] = offset.toString();
if (limit != null) queryParams['limit'] = limit.toString();
```

3. `getStocksReportTyped()` 시그니처도 동일하게 `maxUtime` → `offset, limit` 교체:
```dart
Future<StocksResponse> getStocksReportTyped({
  String? filteringWord,
  Map<String, dynamic>? filters,
  int? offset,
  int? limit,
  String? sortColumn,
  bool? sortAscending,
}) async {
  final response = await getStocksReport(
    filteringWord: filteringWord,
    filters: filters,
    offset: offset,
    limit: limit,
    sortColumn: sortColumn,
    sortAscending: sortAscending,
  );
  return StocksResponse.fromMap(response);
}
```

**database_service.dart 변경:**

4. `getStocksReport()` facade에서 `maxUtime` → `offset, limit` 교체:
```dart
Future<Map<String, dynamic>> getStocksReport({
  String? filteringWord,
  Map<String, dynamic>? filters,
  int? offset,
  int? limit,
  String? sortColumn,
  bool? sortAscending,
}) => _stocksApi.getStocksReport(
  filteringWord: filteringWord,
  filters: filters,
  offset: offset,
  limit: limit,
  sortColumn: sortColumn,
  sortAscending: sortAscending,
);
```

5. `getStocksReportTyped()` facade도 동일하게 교체:
```dart
Future<StocksResponse> getStocksReportTyped({
  String? filteringWord,
  Map<String, dynamic>? filters,
  int? offset,
  int? limit,
  String? sortColumn,
  bool? sortAscending,
}) => _stocksApi.getStocksReportTyped(
  filteringWord: filteringWord,
  filters: filters,
  offset: offset,
  limit: limit,
  sortColumn: sortColumn,
  sortAscending: sortAscending,
);
```

**주의:** `lib/screens/helpers/report_data_loader.dart`에도 `maxUtime` 참조가 있지만 (1260행), 이 파일은 `report_screen_legacy.dart`에서만 import하는 레거시 코드이므로 수정하지 않는다. 컴파일 에러가 발생하면 해당 파일의 `maxUtime:` 호출도 `offset:` / `limit:`로 변경하거나, 레거시 호환을 위해 `maxUtime` 파라미터를 deprecated로 유지할 수 있다. 컴파일 에러 발생 시 레거시 파일도 함께 수정한다.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && grep -n "int? offset" lib/services/api/stocks_api.dart lib/services/database_service.dart && grep -n "int? limit" lib/services/api/stocks_api.dart lib/services/database_service.dart && ! grep -n "maxUtime" lib/services/api/stocks_api.dart && ! grep -n "maxUtime" lib/services/database_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/api/stocks_api.dart` contains `int? offset` (2 occurrences: getStocksReport + getStocksReportTyped)
    - `lib/services/api/stocks_api.dart` contains `int? limit` (2 occurrences)
    - `lib/services/api/stocks_api.dart` does NOT contain `maxUtime`
    - `lib/services/api/stocks_api.dart` contains `queryParams['offset'] = offset.toString()`
    - `lib/services/api/stocks_api.dart` contains `queryParams['limit'] = limit.toString()`
    - `lib/services/database_service.dart` contains `int? offset` (2 occurrences)
    - `lib/services/database_service.dart` contains `int? limit` (2 occurrences)
    - `lib/services/database_service.dart` does NOT contain `maxUtime`
  </acceptance_criteria>
  <done>StocksApi와 DatabaseService의 getStocksReport/getStocksReportTyped가 offset/limit 파라미터를 받아 쿼리 파라미터로 전달한다. maxUtime 관련 코드가 완전히 제거되었다.</done>
</task>

<task type="auto">
  <name>Task 2: StocksReportView 무한스크롤 제거 + 페이지네이션 구현</name>
  <files>lib/screens/reports/stocks_report_view.dart</files>
  <read_first>
    - lib/screens/reports/stocks_report_view.dart (무한스크롤 코드 위치, _loadData 구조, ResizableDataTable 호출부 확인)
    - lib/widgets/resizable_data_table.dart (footerWidget 슬롯 파라미터 확인)
  </read_first>
  <action>
**무한스크롤 코드 제거:**

1. 상태 변수 제거 (53행 부근 `_StocksReportViewState` 클래스 내):
   - `String? _stocksNextMaxUtime;` (69행) 제거
   - `bool _stocksHasMore = false;` (70행) 제거
   - `bool _isLoadingMoreStocks = false;` (71행) 제거

2. `initState()`에서 `_scrollController.addListener(_onScroll);` (84행) 제거

3. `_onScroll()` 메서드 전체 제거 (223~228행)

4. `_loadNextStocksPage()` 메서드 전체 제거 (230~268행)

5. `_reloadDataWithFilters()`에서 무한스크롤 초기화 코드 교체 (197~202행):
```dart
Future<void> _reloadDataWithFilters() async {
  ReportTotalRowBuilder.clearCache();
  // 필터/정렬 변경 시 항상 첫 페이지로 (Pitfall 1 방지)
  _currentPage = 1;
  await _loadData();
}
```

**페이지네이션 상태 변수 추가 (기존 무한스크롤 변수 자리에):**

6. `_StocksReportViewState` 클래스에 추가:
```dart
int _currentPage = 1;        // 현재 페이지 (1-based)
int _pageSize = 100;         // 페이지 크기 (기본 100, per D-05: 50/100/200)
int _totalItems = 0;         // 서버 total (totalPages 계산용)
```

**_loadData() 수정 (147행 부근):**

7. `_databaseService.getStocksReport()` 호출에 offset/limit 추가:
```dart
final data = await _databaseService.getStocksReport(
  filteringWord: currentFilteringWord.isNotEmpty ? currentFilteringWord : null,
  sortColumn: _stocksSortColumn,
  sortAscending: _stocksSortAscending,
  filters: filters.isNotEmpty ? filters : null,
  offset: (_currentPage - 1) * _pageSize,
  limit: _pageSize,
);
```

8. pagination 응답 처리 변경 (169~175행):
```dart
if (data.containsKey('pagination') && data['pagination'] is Map) {
  final pagination = data['pagination'] as Map<String, dynamic>;
  _totalItems = pagination['total'] as int? ?? 0;
}
```

**onSort 콜백에서 페이지 초기화 추가 (452행 부근):**

9. `onSort` 콜백 내에서 `_reloadDataWithFilters()` 호출 전에 이미 `_reloadDataWithFilters()`가 `_currentPage = 1`을 수행하므로 추가 작업 불필요. 기존 코드 유지.

**ResizableDataTable 호출 수정 (438행 부근):**

10. `isLoadingMore: _isLoadingMoreStocks` → `isLoadingMore: false` 변경
11. `footerWidget: _buildPaginationControls()` 추가
12. scrollController 주석 업데이트: `// 세로 스크롤용 (페이지 내 스크롤)`

```dart
return ResizableDataTable(
  columns: StocksBuilder.buildColumnDefs(),
  rows: StocksBuilder.buildRows(dataList),
  columnWidths: mergedColumnWidths,
  onColumnResize: (key, newWidth) {
    setState(() {
      _stocksColumnWidths ??= {};
      _stocksColumnWidths![key] = newWidth;
    });
    StocksColumnWidthStorage.save(_stocksColumnWidthDbKey, _stocksColumnWidths!);
  },
  scrollController: _scrollController, // 세로 스크롤용 (페이지 내 스크롤)
  sortColumn: _stocksSortColumn,
  sortAscending: _stocksSortAscending,
  onSort: (column, ascending) {
    setState(() {
      _stocksSortColumn = column;
      _stocksSortAscending = ascending;
    });
    _notifyStateChanged();
    _reloadDataWithFilters();
  },
  headerColor: stocksColor,
  isLoadingMore: false,
  footerWidget: _buildPaginationControls(),
);
```

**페이지네이션 컨트롤 위젯 메서드 추가 (per D-04, D-05, D-06):**

13. `_buildPaginationControls()` 메서드를 `_reloadDataWithFilters()` 메서드 뒤에 추가:

```dart
/// 페이지네이션 컨트롤: 이전/다음 버튼 + 페이지 번호 + 페이지 크기 선택기 (D-04, D-05, D-06)
Widget _buildPaginationControls() {
  final totalPages = (_totalItems / _pageSize).ceil().clamp(1, 99999);
  return Container(
    padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
    decoration: BoxDecoration(
      border: Border(top: BorderSide(color: Colors.grey[300]!, width: 1)),
    ),
    child: Row(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        // 이전 페이지 버튼
        IconButton(
          icon: const Icon(Icons.chevron_left),
          onPressed: _currentPage > 1 ? () => _goToPage(_currentPage - 1) : null,
        ),
        // 현재 페이지 / 전체 페이지 표시
        Text('$_currentPage / $totalPages'),
        // 다음 페이지 버튼
        IconButton(
          icon: const Icon(Icons.chevron_right),
          onPressed: _currentPage < totalPages ? () => _goToPage(_currentPage + 1) : null,
        ),
        const SizedBox(width: 16),
        // 페이지 크기 선택기 (D-05: 50/100/200)
        DropdownButton<int>(
          value: _pageSize,
          items: [50, 100, 200].map((size) =>
            DropdownMenuItem(value: size, child: Text('$size'))
          ).toList(),
          onChanged: (size) {
            if (size != null) {
              setState(() {
                _pageSize = size;
                _currentPage = 1; // 크기 변경 시 첫 페이지로
              });
              _loadData();
            }
          },
        ),
      ],
    ),
  );
}

/// 지정 페이지로 이동 (스크롤 상단 복귀 포함)
void _goToPage(int page) {
  setState(() => _currentPage = page);
  if (_scrollController.hasClients) {
    _scrollController.jumpTo(0);
  }
  _loadData();
}
```
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && grep -n "_currentPage" lib/screens/reports/stocks_report_view.dart && grep -n "_pageSize" lib/screens/reports/stocks_report_view.dart && grep -n "_totalItems" lib/screens/reports/stocks_report_view.dart && grep -n "_buildPaginationControls" lib/screens/reports/stocks_report_view.dart && grep -n "footerWidget" lib/screens/reports/stocks_report_view.dart && ! grep -n "_onScroll" lib/screens/reports/stocks_report_view.dart && ! grep -n "_loadNextStocksPage" lib/screens/reports/stocks_report_view.dart && ! grep -n "_stocksHasMore" lib/screens/reports/stocks_report_view.dart && ! grep -n "_stocksNextMaxUtime" lib/screens/reports/stocks_report_view.dart && ! grep -n "_isLoadingMoreStocks" lib/screens/reports/stocks_report_view.dart && flutter analyze lib/screens/reports/stocks_report_view.dart lib/services/api/stocks_api.dart lib/services/database_service.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `stocks_report_view.dart` contains `int _currentPage = 1;`
    - `stocks_report_view.dart` contains `int _pageSize = 100;`
    - `stocks_report_view.dart` contains `int _totalItems = 0;`
    - `stocks_report_view.dart` contains `Widget _buildPaginationControls()`
    - `stocks_report_view.dart` contains `void _goToPage(int page)`
    - `stocks_report_view.dart` contains `footerWidget: _buildPaginationControls()`
    - `stocks_report_view.dart` contains `isLoadingMore: false`
    - `stocks_report_view.dart` contains `offset: (_currentPage - 1) * _pageSize`
    - `stocks_report_view.dart` contains `limit: _pageSize`
    - `stocks_report_view.dart` does NOT contain `_onScroll`
    - `stocks_report_view.dart` does NOT contain `_loadNextStocksPage`
    - `stocks_report_view.dart` does NOT contain `_stocksHasMore`
    - `stocks_report_view.dart` does NOT contain `_stocksNextMaxUtime`
    - `stocks_report_view.dart` does NOT contain `_isLoadingMoreStocks`
    - `stocks_report_view.dart` contains `DropdownButton<int>` with items [50, 100, 200]
    - `stocks_report_view.dart` contains `Icons.chevron_left` and `Icons.chevron_right`
    - `flutter analyze` on modified files shows no errors
  </acceptance_criteria>
  <done>무한스크롤 코드 5개(상태 3 + 메서드 2)가 완전히 제거되고, 페이지네이션 상태 3개(_currentPage, _pageSize, _totalItems)와 컨트롤 위젯(_buildPaginationControls)이 추가되었다. 테이블 하단에 이전/다음 + 페이지 번호 + 크기 선택기가 표시된다.</done>
</task>

</tasks>

<verification>
1. `flutter analyze lib/screens/reports/stocks_report_view.dart lib/services/api/stocks_api.dart lib/services/database_service.dart` — 에러 없음
2. `grep -rn "maxUtime" lib/services/` — stocks_api.dart와 database_service.dart에서 결과 없음
3. `grep -rn "_onScroll\|_loadNextStocksPage\|_stocksHasMore\|_stocksNextMaxUtime\|_isLoadingMoreStocks" lib/screens/reports/stocks_report_view.dart` — 결과 없음
4. `grep -n "footerWidget.*_buildPaginationControls" lib/screens/reports/stocks_report_view.dart` — 1건 일치
</verification>

<success_criteria>
- StocksApi/DatabaseService가 offset/limit 파라미터를 사용한다
- StocksReportView에서 무한스크롤 코드가 완전히 제거되었다
- 페이지네이션 컨트롤이 ResizableDataTable footerWidget 슬롯에 삽입되었다
- 필터/정렬 변경 시 _currentPage = 1로 초기화된다
- flutter analyze에서 에러가 없다
</success_criteria>

<output>
After completion, create `.planning/phases/02-table-ux/02-01-SUMMARY.md`
</output>
