---
phase: 04-multi-sucursal-resumen
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/screens/resumen_del_dia_single_sucursal_view.dart
  - lib/screens/resumen_del_dia_multiple_sucursales_view.dart
autonomous: true
requirements: [MSUC-04]

must_haves:
  truths:
    - "단일/카드 뷰의 Stock Resumen 섹션 제목 옆에 지점별 데이터가 아님을 알리는 배지가 표시된다"
    - "비교 테이블 뷰의 Stock Resumen 제목 옆에도 같은 배지가 표시된다 (넓은 화면 우측 패널, 좁은 화면 하단 모두)"
    - "지점 콤보박스에서 특정 sucursal을 선택해도 Stock Resumen 값은 바뀌지 않는다 (배지가 그 이유를 설명한다)"
  artifacts:
    - path: "lib/screens/resumen_del_dia_single_sucursal_view.dart"
      provides: "제목 배지 슬롯을 받는 _buildSection"
      contains: "titleBadge"
    - path: "lib/screens/resumen_del_dia_multiple_sucursales_view.dart"
      provides: "Stock Resumen 제목 옆 전역 배지"
      contains: "DB 전역"
---

<objective>
`stock_resumen`이 지점별 데이터가 아니라 DB 전역 값임을 화면에 명시한다.

Purpose: 지점을 골라도 Stock Resumen 숫자가 그대로이므로, 배지가 없으면 "선택한 지점의 재고"로 오독된다 (D-14).
Output: 두 뷰의 Stock Resumen 섹션 제목 옆에 "DB 전역" 배지.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-sucursal-resumen/04-CONTEXT.md

<interfaces>
From lib/screens/resumen_del_dia_single_sucursal_view.dart:
```dart
// 555행 — 섹션 제목을 그리는 공통 헬퍼. 제목은 Expanded(Text(title)) 안에 있고
// onTap이 있으면 오른쪽에 arrow_forward_ios 아이콘이 붙는다.
Widget _buildSection(BuildContext context, String title, List<Widget> children, bool isLarge,
    {VoidCallback? onTap, bool useGrid = true})

// 520행 — Stock Resumen 섹션 호출부
return _buildSection(context, 'Stock Resumen', stockWidgets, isLarge, useGrid: false, onTap: () {
  onReportTypeSelected(ReportType.stocks);
});
```

From lib/screens/resumen_del_dia_multiple_sucursales_view.dart:
```dart
// 444-455행 (넓은 화면, 우측 패널) 과 547-556행 (좁은 화면, 테이블 아래)
// 두 곳 모두 동일한 형태의 제목 Text 위젯
Text(
  'Stock Resumen',
  style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.grey[800]),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 단일/카드 뷰 Stock Resumen 배지</name>
  <files>lib/screens/resumen_del_dia_single_sucursal_view.dart</files>
  <read_first>
    - lib/screens/resumen_del_dia_single_sucursal_view.dart (483-530행 Stock Resumen 섹션 호출부, 555-590행 `_buildSection` 제목 Row)
  </read_first>
  <action>
1. `_buildSection`(555행) 시그니처에 선택 파라미터 `Widget? titleBadge`를 추가한다:
```dart
Widget _buildSection(BuildContext context, String title, List<Widget> children, bool isLarge,
    {VoidCallback? onTap, bool useGrid = true, Widget? titleBadge})
```

2. 제목 `Row`에서 `Expanded(child: Text(title, ...))` **다음**, `if (onTap != null) Icon(...)` **앞**에 배지를 삽입한다:
```dart
if (titleBadge != null) ...[
  const SizedBox(width: 8),
  titleBadge,
],
```
`Expanded`가 남는 폭을 먹으므로 배지는 아이콘 왼쪽에 붙는다.

3. 배지 위젯을 만드는 private 헬퍼를 `_buildSection` 근처에 추가한다:
```dart
/// 지점별로 나뉘지 않는 전역 데이터임을 알리는 배지 (D-14)
Widget _buildGlobalDataBadge() {
  return Container(
    padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),
    decoration: BoxDecoration(
      color: Colors.amber[50],
      border: Border.all(color: Colors.amber[300]!),
      borderRadius: BorderRadius.circular(10),
    ),
    child: Text(
      'DB 전역',
      style: TextStyle(fontSize: 11, fontWeight: FontWeight.w600, color: Colors.amber[900]),
    ),
  );
}
```

4. 520행의 Stock Resumen `_buildSection` 호출에 `titleBadge: _buildGlobalDataBadge()`를 추가한다. **다른 섹션 호출은 건드리지 않는다** — 배지는 Stock Resumen에만 붙는다.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && grep -n "Widget? titleBadge" lib/screens/resumen_del_dia_single_sucursal_view.dart && grep -n "_buildGlobalDataBadge" lib/screens/resumen_del_dia_single_sucursal_view.dart && grep -c "titleBadge: _buildGlobalDataBadge()" lib/screens/resumen_del_dia_single_sucursal_view.dart && flutter analyze lib/screens/resumen_del_dia_single_sucursal_view.dart 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `_buildSection` signature contains `Widget? titleBadge`
    - file contains `Widget _buildGlobalDataBadge()`
    - file contains `'DB 전역'`
    - `titleBadge: _buildGlobalDataBadge()` appears exactly once (Stock Resumen 섹션에만)
    - the badge is rendered between the title `Expanded` and the `onTap` arrow icon
    - `flutter analyze` on the file reports no errors
  </acceptance_criteria>
  <done>카드 뷰의 Stock Resumen 제목 옆에만 "DB 전역" 배지가 표시되고, 다른 섹션 제목은 변경되지 않았다.</done>
</task>

<task type="auto">
  <name>Task 2: 비교 테이블 뷰 Stock Resumen 배지</name>
  <files>lib/screens/resumen_del_dia_multiple_sucursales_view.dart</files>
  <read_first>
    - lib/screens/resumen_del_dia_multiple_sucursales_view.dart (440-460행 넓은 화면 우측 패널 제목, 526-560행 좁은 화면 하단 제목)
  </read_first>
  <action>
1. 파일에 배지 헬퍼를 추가한다 (04-01에서 만든 헬퍼와 동일한 모양 — 두 파일은 서로 import하지 않으므로 각자 갖는다):
```dart
/// 지점별로 나뉘지 않는 전역 데이터임을 알리는 배지 (D-14)
Widget _buildGlobalDataBadge() { ... }  // 04-02 Task 1과 동일한 구현
```

2. **두 곳** 모두에서 `Text('Stock Resumen', ...)`을 `Row`로 감싸 배지를 오른쪽에 붙인다:
   - 넓은 화면 우측 패널: 444-455행 부근 (`Padding(padding: EdgeInsets.only(bottom: 16), child: Text('Stock Resumen', ...))`)
   - 좁은 화면 하단: 547-556행 부근

```dart
Row(
  mainAxisSize: MainAxisSize.min,
  children: [
    Text(
      'Stock Resumen',
      style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.grey[800]),
    ),
    const SizedBox(width: 8),
    _buildGlobalDataBadge(),
  ],
)
```
바깥의 `Padding`과 `Center` 정렬은 그대로 유지한다.
  </action>
  <verify>
    <automated>cd /Users/marcoskim/Trabajos_Programming/flutter_aguila && grep -n "_buildGlobalDataBadge" lib/screens/resumen_del_dia_multiple_sucursales_view.dart && test $(grep -c "_buildGlobalDataBadge()," lib/screens/resumen_del_dia_multiple_sucursales_view.dart) -eq 2 && grep -n "DB 전역" lib/screens/resumen_del_dia_multiple_sucursales_view.dart && flutter analyze lib/screens/resumen_del_dia_multiple_sucursales_view.dart 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - file contains `Widget _buildGlobalDataBadge()`
    - file contains `'DB 전역'`
    - `_buildGlobalDataBadge(),` is called exactly twice (넓은 화면 우측 패널 + 좁은 화면 하단)
    - both `Text('Stock Resumen', ...)` occurrences are now inside a `Row` with the badge
    - `flutter analyze` on the file reports no errors
  </acceptance_criteria>
  <done>비교 테이블 뷰의 두 Stock Resumen 제목(넓은 화면/좁은 화면) 모두에 "DB 전역" 배지가 붙었다.</done>
</task>

</tasks>

<verification>
1. `flutter analyze lib/` — 에러 없음
2. `grep -c "DB 전역" lib/screens/resumen_del_dia_single_sucursal_view.dart lib/screens/resumen_del_dia_multiple_sucursales_view.dart` — 각 1건 이상
3. `grep -c "titleBadge: _buildGlobalDataBadge()" lib/screens/resumen_del_dia_single_sucursal_view.dart` — 정확히 1건
4. `grep -c "_buildGlobalDataBadge()," lib/screens/resumen_del_dia_multiple_sucursales_view.dart` — 정확히 2건
</verification>

<success_criteria>
- 두 뷰의 Stock Resumen 제목 옆에 "DB 전역" 배지가 표시된다
- 다른 섹션 제목에는 배지가 붙지 않았다
- `flutter analyze lib/`에서 에러가 없다
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-sucursal-resumen/04-02-SUMMARY.md`
</output>
